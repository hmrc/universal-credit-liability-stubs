meta {
  name: _lib
  seq: 1
}

docs {
  An auxiliary folder for JavaScript helpers.

  Make sure to enable [Developer Mode](https://docs.usebruno.com/configure/javascript-sandbox).

  Example:
  ```
  const helpers = require('./_lib/helpers')
  const nino = helpers.randomNino()
  ```

}
